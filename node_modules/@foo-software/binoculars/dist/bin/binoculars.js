#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const meow_1 = __importDefault(require("meow"));
const binoculars_1 = __importDefault(require("../binoculars"));
const cli = meow_1.default();
const { author, awsAccessKeyId, awsBucket, awsRegion, awsSecretAccessKey, branch, commentAccessToken, commentUrl, enableComments, locale, minScore, outputDirectory, pr, sha, slackWebhookUrl, url, urls, } = cli.flags;
const run = async () => {
    try {
        const urlList = typeof urls !== 'string' ? undefined : urls.split('|');
        await binoculars_1.default({
            author,
            awsAccessKeyId,
            awsBucket,
            awsRegion,
            awsSecretAccessKey,
            branch,
            commentAccessToken,
            commentUrl,
            enableComments,
            locale,
            minScore,
            outputDirectory,
            pr,
            sha,
            slackWebhookUrl,
            url,
            urls: urlList,
        });
    }
    catch (error) {
        console.error(error);
        process.exit(1);
    }
    process.exit(0);
};
run();
//# sourceMappingURL=binoculars.js.map